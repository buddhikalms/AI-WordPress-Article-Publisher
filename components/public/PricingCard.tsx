import Link from "next/link";
import { CheckCircle2 } from "lucide-react";
import { cn } from "@/lib/utils";

export default function PricingCard({ name, price, description, features, featured = false }: { name: string; price: string; description: string; features: readonly string[]; featured?: boolean }) {
  return <article className={cn("relative h-full rounded-[26px] border p-6", featured ? "border-blue-600 bg-slate-950 text-white shadow-2xl shadow-blue-900/15" : "border-slate-200 bg-white")}>{featured ? <span className="absolute right-5 top-5 rounded-full bg-blue-600 px-3 py-1 text-[9px] font-bold uppercase tracking-widest text-white">Most popular</span> : null}<p className={cn("text-xs font-semibold", featured ? "text-blue-300" : "text-blue-700")}>{name}</p><div className="mt-5 flex items-end gap-1"><span className={cn("text-4xl font-semibold tracking-tight", featured ? "text-white" : "text-slate-950")}>{price}</span><span className="pb-1 text-xs text-slate-400">/month</span></div><p className={cn("mt-4 min-h-12 text-xs leading-6", featured ? "text-slate-400" : "text-slate-600")}>{description}</p><div className={cn("my-6 h-px", featured ? "bg-slate-800" : "bg-slate-200")} /><ul className="space-y-3">{features.map((feature) => <li key={feature} className={cn("flex items-center gap-2 text-xs", featured ? "text-slate-300" : "text-slate-700")}><CheckCircle2 className={cn("h-3.5 w-3.5", featured ? "text-blue-400" : "text-emerald-600")} />{feature}</li>)}</ul><Link href="/register" className={cn("mt-8 block rounded-xl px-4 py-3 text-center text-xs font-semibold", featured ? "bg-blue-600 text-white hover:bg-blue-500" : "border border-slate-200 text-slate-800 hover:bg-blue-50")}>Start Free</Link></article>;
}
